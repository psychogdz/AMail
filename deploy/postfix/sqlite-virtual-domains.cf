# Postfix SQLite map for virtual domain validation
# Path: /etc/postfix/sqlite-virtual-domains.cf

dbpath = /var/www/amail/db.sqlite3
query = SELECT 1 FROM mailboxes_emailaddress WHERE domain='%s' AND is_active=1 LIMIT 1
